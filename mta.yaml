_schema-version: "3.2"
ID: northwind
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: northwind-destination-content
  type: com.sap.application.content
  requires:
  - name: northwind-destination-service
    parameters:
      content-target: true
  - name: northwind-repo-host
    parameters:
      service-key:
        name: northwind-repo-host-key
  - name: northwind-uaa
    parameters:
      service-key:
        name: northwind-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: northwind_html_repo_host
          ServiceInstanceName: northwind-html5-service
          ServiceKeyName: northwind-repo-host-key
          sap.cloud.service: northwind
        - Authentication: OAuth2UserTokenExchange
          Name: northwind_uaa
          ServiceInstanceName: northwind-xsuaa-service
          ServiceKeyName: northwind-uaa-key
          sap.cloud.service: northwind
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: northwind-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: northwind-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - northwind.zip
      name: northwind
      target-path: resources/
- name: northwind
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: northwind-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: northwind-destination-service
    service-plan: lite
- name: northwind-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: northwind-xsuaa-service
    service-plan: application
- name: northwind-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: northwind-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
